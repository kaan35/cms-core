{
  "name": "cms-core",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "plugins/*/api",
    "plugins/*/admin",
    "templates/*",
    "cli"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=templates/api",
    "dev:admin": "npm run dev --workspace=templates/admin",
    "dev:client": "npm run dev --workspace=templates/client",
    "type-check": "npm run type-check --workspace=templates/api && npm run type-check --workspace=templates/admin",
    "type-check:api": "npm run type-check --workspace=templates/api",
    "type-check:admin": "npm run type-check --workspace=templates/admin"
  },
  "devDependencies": {
    "@types/node": "^26",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "babel-plugin-react-compiler": "^1",
    "tsx": "^4"
  }
}
